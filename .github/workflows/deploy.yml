name: Build and Deploy project

on:
  push:
    branches: ["master"]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: bonk-inc/babs

jobs:
  build:
    name: Build & push image to registry
    runs-on: ubuntu-latest
    permissions: 
      contents: read
      packages: write
      
    steps:
      - name: 'Checkout Github Action'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Create frontend .env file
        run: |
          cd frontend/ & cat <<EOT >> .env
          VITE_APP_API_URL=${{ vars.APP_API_URL }}
          VITE_APP_AUTH_URL=${{ vars.APP_AUTH_URL }}
          VITE_APP_AUTH_CLIENT_ID=${{ vars.APP_AUTH_CLIENT_ID }}
          VITE_APP_AUTH_REDIRECT_URL=${{ vars.APP_AUTH_REDIRECT_UR }}
          EOT

      - name: test
        run: ls -la frontend/

      - name: Log into registry ${{ env.REGISTRY }}
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        id: build-and-push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to Coolify
        run: | 
          curl --request GET '${{ secrets.COOLIFY_WEBHOOK }}' --header 'Authorization: Bearer ${{ secrets.COOLIFY_TOKEN }}'